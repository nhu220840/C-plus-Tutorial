#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main(){
    int n, k; cin >> n >> k;
    int a[n];
    for(int &x : a) cin >> x;
    int l = 0;
    ll cnt = 0;
    for(int r = 0; r < n; r++){
        while()
    }
}